#include <stdio.h>
#include <math.h>

// remember that integer math operations are included in <stdlib.h>

int main() {
    double a = 100.0;
    double res = sqrt(a);
    printf("Result of sqrt(100.0) = %f", res);
    return 0;
}
